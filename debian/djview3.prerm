#! /bin/sh

case "$1" in
    remove|deconfigure)
	# Alternatives
	if [ -x /usr/sbin/update-alternatives ] ; then
	    /usr/sbin/update-alternatives --quiet \
		--remove djview /usr/bin/djview3
	fi
	;;
esac

#DEBHELPER#
