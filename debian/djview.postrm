#! /bin/sh

# Alternatives
if [ "$1" = "remove" ] && [ -x /usr/sbin/update-alternatives ] ; then
  /usr/sbin/update-alternatives --quiet --remove djview /usr/bin/djview3
fi

# Menu entries
if [ "$1" = "remove" ] && 
   [ -x /usr/bin/update-desktop-database ] ; then
  /usr/bin/update-desktop-database
fi

#DEBHELPER#
